import("//build/image/image.gni")

source_set("hf_test_vm") {
  sources = [
    "vm_entry.S",
  ]

  deps = [
    "//src:common",
    "//src:common_debug",
    "//src/arch/${arch}:entry",
    "//src/arch/${arch}/vm:hf_call",
    "//src/arch/${arch}/vm:shutdown",
  ]
}
